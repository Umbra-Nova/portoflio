# Pin exact versions instead of ^ranges when installing — QA tooling (eslint,
# playwright, browser binaries) should be reproducible across dev machines
# and CI rather than silently drifting on patch/minor releases.
save-exact=true

# Keep npm install output focused on real problems.
fund=false
audit-level=moderate
